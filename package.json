{
  "name": "billy777-bulk-messaging",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "predev": "node -e \"const kp=require('kill-port'); Promise.all([kp(8080),kp(8000),kp(8001)]).catch(()=>{}).finally(()=>process.exit(0));\"",
    "dev": "concurrently --kill-others-on-fail --names \"BACKEND,FRONTEND\" --prefix-colors \"green,cyan\" \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "setup": "npm install --prefix backend && npm install --prefix frontend",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "kill-port": "^2.0.1"
  }
}
